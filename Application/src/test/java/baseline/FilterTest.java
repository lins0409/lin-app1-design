package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class FilterTest {
    @Test
    void testFilers(){
        //add some tasks to the list, make some of them completed
        //make sure that when the booleans are set to true or false, that it uses the right functions
        //i.e make sure it only returns the completed tasks
    }
}