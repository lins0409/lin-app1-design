package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class TaskTest {

    @Test
    void addTaskTest() {
        //add the task, description, date, etc seperately here
        //make sure that the method returns the right method
    }

    @Test
    void editTaskTest() {
        //add the task
        //then make a change to the task
        //make sure the returned value is the now editted task
    }

    @Test
    void deleteTaskTest() {
        //add some tasks
        //then delete a task off of the list
        //make sure that the spot where the task was is now empty
    }
}